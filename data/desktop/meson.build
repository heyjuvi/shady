desktop_dir = join_paths(get_option('datadir'), 'applications')
icon_dir = join_paths(get_option('datadir'), 'pixmaps')

install_data([
  'org.hasi.shady.desktop',
], install_dir: desktop_dir)

install_data([
  'shady.svg',
], install_dir: icon_dir)
